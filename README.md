Create a virtual environment "rag_env" and pip install all the libraries in the requirements.txt file.
run app.py to start the indexing process once done, querying can be done on the docs. for now the file path is hardcoded in the app.py file.
